User-agent: *
Disallow: /

# Indexing is deliberately blocked until a custom domain is wired up.
# Indexing the .web.app URL first would create duplicate-content issues
# when the custom domain goes live. Flip this and `meta name="robots"`
# in index.html together when the production domain is configured.
